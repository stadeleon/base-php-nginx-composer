# base-php-nginx-composer

to start server 
copy .env.template to .env

and run
`docker-compose up -d`

after containers are started

open http://0.0.0.0:8000 to check server is started and php is configured
